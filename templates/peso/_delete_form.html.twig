<form method="post" action="{{ path('app_peso_delete', {'id': peso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ peso.id) }}">
    <button class="btn">Delete</button>
</form>
