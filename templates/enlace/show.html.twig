{% extends 'base.html.twig' %}

{% block title %}Enlace{% endblock %}

{% block body %}
    <h1>Enlace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enlace.id }}</td>
            </tr>
            <tr>
                <th>Enlace</th>
                <td>{{ enlace.enlace }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enlace_index') }}">back to list</a>

    <a href="{{ path('app_enlace_edit', {'id': enlace.id}) }}">edit</a>

    {{ include('enlace/_delete_form.html.twig') }}
{% endblock %}
