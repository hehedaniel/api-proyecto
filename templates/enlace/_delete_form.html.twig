<form method="post" action="{{ path('app_enlace_delete', {'id': enlace.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enlace.id) }}">
    <button class="btn">Delete</button>
</form>
